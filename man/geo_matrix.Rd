% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geo-matrix.R
\name{geo_matrix}
\alias{geo_matrix}
\title{Retrieve Series Matrix and Create ExpressionSet}
\usage{
geo_matrix(
  accession,
  add_gpl = NULL,
  pdata_from_soft = FALSE,
  ftp_over_https = NULL,
  handle_opts = list(),
  odir = getwd()
)
}
\arguments{
\item{accession}{A character of GEO accession IDs. Examples:
\itemize{
\item DataSets (GDS): \code{"GDS505"}, \code{"GDS606"}, \code{"GDS1234"}, \code{"GDS9999"}, etc.
\item Series (GSE): \code{"GSE2"}, \code{"GSE22"}, \code{"GSE100"}, \code{"GSE2000"}, etc.
\item Platforms (GPL): \code{"GPL96"}, \code{"GPL570"}, \code{"GPL10558"}, etc.
\item Samples (GSM): \code{"GSM12345"}, \code{"GSM67890"}, \code{"GSM112233"}, etc.
}}

\item{add_gpl}{Logical or \code{NULL}. Whether to include platform information
(the \code{\link[Biobase:featureData]{featureData}} slot). If \code{NULL} (default), the
function attempts to map the GPL accession to a Bioconductor annotation
package. If successful, the \code{\link[Biobase:eSet]{annotation}} slot is updated
and \code{add_gpl} is set to \code{FALSE}; otherwise, \code{add_gpl} is set to \code{TRUE}.}

\item{pdata_from_soft}{Logical. Specifies whether to derive \code{phenoData} from
the GSE series SOFT file. Defaults to \code{FALSE}, in which case \code{phenoData} is
parsed directly from the series matrix file. Set to \code{TRUE} if you encounter
issues parsing \verb{characteristics_ch*} columns correctly, as it will attempt
to retrieve the data from the SOFT file instead.}

\item{ftp_over_https}{Logical scalar. If \code{TRUE}, connects to GEO FTP server
via HTTPS (\url{https://ftp.ncbi.nlm.nih.gov/geo}); otherwise uses plain FTP
(\url{ftp://ftp.ncbi.nlm.nih.gov/geo}). Only applicable to GEO FTP server
access.}

\item{handle_opts}{A list of named options / headers to be set in the
\code{\link[curl:multi_download]{multi_download}}.}

\item{odir}{Destination directory for downloads. Defaults to the current
working directory.}
}
\value{
An \code{\link[Biobase:ExpressionSet]{ExpressionSet}} or a list of
\code{ExpressionSet}s, one per Series Matrix file.
}
\description{
The function downloads and parses the relevant Series Matrix files,
optionally mapping platform IDs to Bioconductor annotation packages.
}
\examples{
\donttest{
if (require("Biobase")) {
    eset <- geo_matrix("GSE10", odir = tempdir())
}
}
}
